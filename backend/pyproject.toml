[project]
name = "backend"
version = "0.1.0"
description = ""
authors = [
    {name = "OlehKapat",email = "bombastik@ukr.net"}
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard] (>=0.135.1,<0.136.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
